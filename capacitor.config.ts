import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'yalies.me',
  appName: 'Yalies Around Me',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
